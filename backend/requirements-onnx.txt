# ONNX classifier runtime for the Snapdragon X Elite hub.
numpy>=2.0
onnx>=1.17

# Hexagon NPU ONNX Execution Provider (win-arm64 only). onnxruntime-qnn 2.x is
# a plugin EP package that pins a matching base onnxruntime and ships the
# QnnHtp DLLs. On non-Snapdragon dev machines install plain onnxruntime instead.
onnxruntime-qnn>=1.17.0    # win-arm64 (Snapdragon X Elite)
# onnxruntime>=1.17.0      # x86/other fallback (use instead of onnxruntime-qnn)

# QUAD client SDK: NPU session helpers (ort_qnn) + MCP tool access for
# conversion/profiling workflows. Not yet on PyPI; install from source:
#   pip install <path-to-QUAD-Client checkout>
# quad-mcp-client
fastmcp>=2.0
httpx>=0.27
psutil>=5.9
